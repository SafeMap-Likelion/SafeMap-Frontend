import React from 'react';
import MyPostDetailScreen from '../../features/mypage/MyPostDetailScreen';

/**
 * '/(main)/mypost-detail' 경로에 대한 라우트 파일입니다.
 * 마이페이지에서 접근하는 게시물 상세 화면 (수정 버튼 포함)
 */
export default function MyPostDetailPage() {
  return <MyPostDetailScreen />;
}
