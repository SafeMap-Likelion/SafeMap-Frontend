import React from 'react';
import PostEditScreen from '../../features/mypage/PostEditScreen';

/**
 * '/(main)/post-edit' 경로에 대한 라우트 파일입니다.
 * 게시물 수정 화면
 */
export default function PostEditPage() {
  return <PostEditScreen />;
}
